logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

spring:
  profiles:
    active:
      - local
      - common
    group:
      local:
        - db-local
        - security-local
      prob:
        - db-prod
        - security-proda
    include:
      - db
      - security
